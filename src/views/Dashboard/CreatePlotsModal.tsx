import React from 'react';
import { ModalProps } from '../../components/Modal';
import { CreateAPlotModal } from '../../components/Modal/Modal';

const CreatePlotsModal: React.FC<ModalProps> = ({ onDismiss }) => {
  return <CreateAPlotModal />;
};

export default CreatePlotsModal;
